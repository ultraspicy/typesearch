//! Query builder types

use serde_json::Value;

pub struct QueryBuilder {
    _query: Value,
}

impl QueryBuilder {
    /// Create a new query builder
    pub fn new() -> Self {
        Self {
            _query: serde_json::json!({}),
        }
    }
}

impl Default for QueryBuilder {
    fn default() -> Self {
        Self::new()
    }
}

// TODO: Implement type-safe query builder
// This will be generated by the derive macro in future versions
